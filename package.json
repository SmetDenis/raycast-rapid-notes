{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "rapid-notes",
  "title": "Rapid Notes",
  "description": "Quickly capture notes: append a checklist item or a block under a heading, or create a new timestamped task or note file with YAML frontmatter — instantly or via an editable form.",
  "icon": "icon.png",
  "author": "smetdenis",
  "categories": [
    "Productivity"
  ],
  "license": "MIT",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "commands": [
    {
      "name": "append-checklist",
      "title": "Append Checklist",
      "description": "Instantly append the selected or typed text as a checklist item under a heading in your Markdown file.",
      "mode": "no-view",
      "preferences": [
        { "name": "checklistFile", "title": "File", "description": "Markdown file this command writes to.", "type": "file", "required": false },
        { "name": "checklistHeading", "title": "Heading", "description": "Append under this heading (case-insensitive). Leading #s set the level, e.g. '## Tasks'; plain text is H1; leave empty to append to the end of the file.", "type": "textfield", "required": false, "default": "" },
        { "name": "checklistTemplate", "title": "Template", "description": "Template per appended item. Placeholders: {content} {url} {title} {app} {project} {page} {tags} {date} {time} {datetime}; formatted (label + newline): {content_f} {url_f} {title_f} {app_f} {project_f} {page_f} {tags_f}.", "type": "textfield", "required": false, "default": "- [ ] **{date} {time}**: {content}" }
      ],
      "arguments": [
        {
          "name": "text",
          "type": "text",
          "placeholder": "Checklist text",
          "required": false
        },
        {
          "name": "project",
          "type": "text",
          "placeholder": "Project",
          "required": false
        }
      ]
    },
    {
      "name": "append-note",
      "title": "Append Note",
      "description": "Instantly append the selected or typed text as a block under a heading in your notes file.",
      "mode": "no-view",
      "preferences": [
        { "name": "appendNoteFile", "title": "File", "description": "Markdown file this command writes to.", "type": "file", "required": false },
        { "name": "appendNoteHeading", "title": "Heading", "description": "Append under this heading (case-insensitive). Leading #s set the level; plain text is H1; leave empty to append to the end of the file.", "type": "textfield", "required": false, "default": "" },
        { "name": "appendNoteTemplate", "title": "Template", "description": "Template per appended block. Same placeholders as the checklist template (incl. {tags}/{tags_f}).", "type": "textfield", "required": false, "default": "{content}\\n\\n_{date} {time} · {app}_" }
      ],
      "arguments": [
        {
          "name": "text",
          "type": "text",
          "placeholder": "Note text",
          "required": false
        },
        {
          "name": "project",
          "type": "text",
          "placeholder": "Project",
          "required": false
        }
      ]
    },
    {
      "name": "new-task",
      "title": "New Task",
      "description": "Instantly create a new task file with YAML frontmatter in your task directory.",
      "mode": "no-view",
      "preferences": [
        { "name": "taskDirectory", "title": "Directory", "description": "Folder where new task files are created.", "type": "directory", "required": false },
        { "name": "taskTemplate", "title": "Body Template", "description": "Template for the task body; YAML frontmatter is added automatically. Same placeholders as the append templates (incl. {tags}/{tags_f}).", "type": "textfield", "required": false, "default": "{content}" },
        { "name": "taskFrontmatter", "title": "Frontmatter", "description": "Extra YAML frontmatter as 'key: value; key2: value2'. Reserved keys (created, title, project, tags, source_url, updated) are set automatically and rejected here.", "type": "textfield", "required": false, "default": "type: task; task_status: active" },
        { "name": "defaultTags", "title": "Default Tags", "description": "Comma-separated tags written to new task files (and available as {tags}).", "type": "textfield", "required": false, "default": "" }
      ],
      "arguments": [
        {
          "name": "text",
          "type": "text",
          "placeholder": "Task text",
          "required": false
        },
        {
          "name": "project",
          "type": "text",
          "placeholder": "Project",
          "required": false
        },
        {
          "name": "title",
          "type": "text",
          "placeholder": "Title",
          "required": false
        }
      ]
    },
    {
      "name": "new-note",
      "title": "New Note",
      "description": "Instantly create a new note file with YAML frontmatter in your note directory.",
      "mode": "no-view",
      "preferences": [
        { "name": "noteDirectory", "title": "Directory", "description": "Folder where new note files are created.", "type": "directory", "required": false },
        { "name": "noteTemplate", "title": "Body Template", "description": "Template for the note body; YAML frontmatter is added automatically. Same placeholders as the append templates (incl. {tags}/{tags_f}).", "type": "textfield", "required": false, "default": "{content}\\n\\n{page_f}" },
        { "name": "noteFrontmatter", "title": "Frontmatter", "description": "Extra YAML frontmatter as 'key: value; key2: value2'. Reserved keys (created, title, project, tags, source_url, updated) are set automatically and rejected here.", "type": "textfield", "required": false, "default": "type: note" },
        { "name": "defaultTags", "title": "Default Tags", "description": "Comma-separated tags written to new note files (and available as {tags}).", "type": "textfield", "required": false, "default": "" }
      ],
      "arguments": [
        {
          "name": "text",
          "type": "text",
          "placeholder": "Note text",
          "required": false
        },
        {
          "name": "project",
          "type": "text",
          "placeholder": "Project",
          "required": false
        },
        {
          "name": "title",
          "type": "text",
          "placeholder": "Title",
          "required": false
        }
      ]
    },
    {
      "name": "rapid-note",
      "title": "Rapid Note",
      "description": "Capture with an editable form: append under a heading, or create a new task/note file — your choice, edited before saving.",
      "mode": "view",
      "preferences": [
        { "name": "appendFile", "title": "Append: File", "description": "Markdown file the form appends to when 'Append' is selected.", "type": "file", "required": false },
        { "name": "appendHeading", "title": "Append: Heading", "description": "Append under this heading (case-insensitive). Leading #s set the level; plain text is H1; empty appends to the end of the file.", "type": "textfield", "required": false, "default": "" },
        { "name": "appendTemplate", "title": "Append: Template", "description": "Template for the appended block. Placeholders incl. {content} {page} {tags} {date} {time} and their {*_f} forms.", "type": "textfield", "required": false, "default": "{content}\\n\\n_{date} {time} · {app}_" },
        { "name": "createDirectory", "title": "Create: Directory", "description": "Folder where the form creates a new file when 'Create' is selected.", "type": "directory", "required": false },
        { "name": "createTemplate", "title": "Create: Body Template", "description": "Template for the created file body; YAML frontmatter is added automatically.", "type": "textfield", "required": false, "default": "{content}\\n\\n{page_f}" },
        { "name": "createFrontmatter", "title": "Create: Frontmatter", "description": "Extra YAML frontmatter as 'key: value; key2: value2'. Reserved keys are set automatically and rejected here.", "type": "textfield", "required": false, "default": "type: note" },
        { "name": "defaultTags", "title": "Default Tags", "description": "Comma-separated tags prefilled into the form's Tags field.", "type": "textfield", "required": false, "default": "" }
      ]
    }
  ],
  "preferences": [
    {
      "name": "dateFormat",
      "title": "Date Format",
      "description": "date-fns format for {datetime} and the frontmatter created field.",
      "type": "textfield",
      "required": false,
      "default": "yyyy-MM-dd'T'HH:mm:ss"
    },
    {
      "name": "filenameDateFormat",
      "title": "Filename Date Format",
      "description": "date-fns format for new task/note filenames. Avoid characters invalid in filenames.",
      "type": "textfield",
      "required": false,
      "default": "yyyy-MM-dd-HHmmss"
    },
    {
      "name": "mergeSeparator",
      "title": "Argument + Selection Separator",
      "description": "How a typed argument joins with the selected/clipboard text when both are present.",
      "type": "dropdown",
      "required": false,
      "default": "semicolon",
      "data": [
        {
          "title": "Semicolon",
          "value": "semicolon"
        },
        {
          "title": "Space",
          "value": "space"
        },
        {
          "title": "Newline",
          "value": "newline"
        }
      ]
    },
    {
      "name": "clipboardFallback",
      "title": "Clipboard Fallback",
      "label": "Use the clipboard when no text is selected",
      "description": "When on, commands fall back to the clipboard if nothing is selected — for apps like Telegram. Copy the text first, then run the command.",
      "type": "checkbox",
      "required": false,
      "default": false
    }
  ],
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "publish": "npx @raycast/api@latest publish",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@raycast/api": "^1.104.21",
    "date-fns": "^4.4.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.2.0",
    "@types/node": "^26.1.0",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "eslint": "^9.39.4",
    "prettier": "^3.9.4",
    "typescript": "^5.9.3",
    "vitest": "^4.1.9"
  }
}
